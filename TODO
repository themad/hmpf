Haskell:
 - search - partially done, can't limit search to fields yet
   caching long searches for pagination.
 - configuration
   interface, port
   MPD_HOST already supported


JS:
 - search, if done in haskell

HTML/CSS:
 - playse buttons (pause bigger on the outside)
